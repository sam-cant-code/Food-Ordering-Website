import React, { useState } from 'react'
import './Cart.css'

const Cart = () => {

  const {cartItems, food_list, removeFromCart} = useState(StoreContext);

  return (
    <div>
      
    </div>
  )
}

export default Cart
